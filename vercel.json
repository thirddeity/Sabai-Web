{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun --bun run --cwd apps/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
